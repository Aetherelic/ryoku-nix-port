# xdg-desktop-portal-hyprland config. The shell owns the screen-share picker:
# the portal runs ryoku-share as its custom picker, which asks the shell daemon
# to open the in-frame picker and prints the chosen source. The picker replies
# in the portal's own wire format; ryoku-share only forwards it.
screencopy {
    custom_picker_binary = ryoku-share
}
